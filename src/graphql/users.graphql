query users($page: Int, $filter: UsersFilter, $skip: Int) {
  users(page: $page, filter: $filter, skip: $skip) {
    data {
      id
      firstName
      lastName
      avatar
      isFriend
    }
    nextPage
  }
}
